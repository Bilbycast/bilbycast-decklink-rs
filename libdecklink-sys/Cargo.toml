[package]
name = "libdecklink-sys"
version = "0.3.0"
edition = "2021"
license = "MPL-2.0"
publish = false
description = "Raw FFI to a C ABI shim over the Blackmagic DeckLink SDK (capture + playout)"

[lib]
doctest = false

[build-dependencies]
bindgen = "0.72.1"
cc = "1"
